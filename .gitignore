# Ignore dependencies and build cache in source
source/node_modules
source/.next
source/out

# Ignore root node_modules if any
node_modules
_next

# Keep built files in root (index.html, etc) for GitHub Pages
!index.html
!404.html
!favicon.ico
!images/
!_next/static/
